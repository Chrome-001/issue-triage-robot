# Copy to .env / export. Token needs issues read + write on the target repos.
GH_TOKEN=ghp_your_token_here

# Comma-separated owner/repo pairs to triage
GITHUB_REPOS=Chrome-001/repo-one,Chrome-001/repo-two

# Optional tuning (matches the CLI flags)
# MODE=all
# DAYS=30
# CLOSE_DAYS=60